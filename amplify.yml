version: 1
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - npm i next
        - npm run build
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []