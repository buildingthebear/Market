version: 1
frontend:
  phases:
    preBuild:
      commands:
        - /bin/bash -c "pip3 install mkdocs --user --no-cache-dir"
    build:
      commands:
        - /bin/bash -c "mkdocs build"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []